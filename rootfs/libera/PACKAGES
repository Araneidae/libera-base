# Package definitions for libera (see also CONFIG.common)

# Components required for normal operation.
PACKAGES += ntp dropbear portmap

# Useful debugging tools
PACKAGES += strace lsof
# Needed for subsequence maintenance and possible upgrades
PACKAGES += mtd-utils 
# Useful for network analysis.
PACKAGES += libpcap tcpdump

# Dropbear is a little slow when built with thumb, so we override that.
CFLAGS_dropbear = -O2

# Package specific settings

# busybox:
# Enable inetd: may want to configure httpd at some point.
INETD_ENABLE = y


# dropbear:
# Authorised keys to be installed when installing dropbear: allows root
# access without passwords for the following users:
#
#   gr58    Guenther Rehm
#   mga83   Michael Abbott
#   iu42    Isa Uzun
#   jr76    James Rowland
#   vck53   Vince Kempson
#   asd53   Andrew Dixon
#   rb64    Riccardo Bartolini
#   bs36    Beni Singh
#   cc83    Chris Cristou
#
SSH_AUTHORIZED_KEYS = $(configdir)/authorized_keys
# All Liberas are installed with the same ssh host key.  This will save
# annoyances when moving machines around.
DROPBEAR_KEYS = $(foreach key,dss rsa,$(configdir)/dropbear_$(key)_host_key)
