# Configuration file for libera target.
include $(TOP)/CONFIG
include $(configdir)/../common/CONFIG.common
include $(configdir)/PACKAGES
include $(configdir)/SYSROOT
include $(configdir)/BOOT_$(BOOT)
