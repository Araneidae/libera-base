# Configuration options common for all u-boot options (intramfs and nfs).

BOOT_LOADER = u-boot

MKIMAGE = $(U_BOOT_TOP)/mkimage

KERNEL_NAME = uImage-libera-$(KERNEL_RELEASE)
KERNEL_ADDR = a0001000
BOOTARGS = console=ttyS0,115200 mem=64M
